class Solution {
public:
    int strStr(string haystack, string needle) {
        size_t pos = haystack.find(needle);
        if(needle.empty())
            return 0;
        else return pos;
    }
};
